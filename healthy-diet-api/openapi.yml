openapi: 3.0.3
info:
  title: Healthy Diet API
  version: 1.0.0
  description: API specification synchronized with current Axum routes.
servers:
  - url: http://localhost:3000
    description: Local development

tags:
  - name: System
  - name: Authentication
  - name: Users
  - name: Nutrition
  - name: Diet
  - name: Chat
  - name: Knowledge
  - name: Admin

paths:
  /:
    get:
      tags: [System]
      summary: Root health text
      operationId: root
      responses:
        '200':
          description: Connect success message
          content:
            text/plain:
              schema:
                type: string
                example: Connect Success!

  /openapi.yml:
    get:
      tags: [System]
      summary: Get OpenAPI YAML document
      operationId: getOpenApiYaml
      responses:
        '200':
          description: OpenAPI specification in YAML format
          content:
            application/yaml:
              schema:
                type: string

  /ping:
    get:
      tags: [System]
      summary: Ping server
      operationId: ping
      responses:
        '200':
          description: Meme-style ping text
          content:
            text/plain:
              schema:
                type: string

  /health:
    get:
      tags: [System]
      summary: Health check
      operationId: healthCheck
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /gemma4/health:
    get:
      tags: [System]
      summary: Check local Gemma4 service reachability
      operationId: gemma4HealthCheck
      responses:
        '200':
          description: Gemma4 service is reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gemma4HealthResponse'
        '503':
          description: Gemma4 service is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gemma4HealthResponse'

  /auth/discord/login:
    get:
      tags: [Authentication]
      summary: Redirect to Discord OAuth login
      operationId: discordLogin
      responses:
        '307':
          description: Temporary redirect to Discord OAuth authorize URL

  /auth/discord/callbck:
    get:
      tags: [Authentication]
      summary: Discord OAuth callback
      operationId: discordCallback
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
        - in: query
          name: state
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Discord login result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscordLoginResponse'

  /auth/register:
    post:
      tags: [Authentication]
      summary: Register account
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterPayload'
      responses:
        '200':
          description: Register success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Email already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags: [Authentication]
      summary: Login
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginPayload'
      responses:
        '200':
          description: Login success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid email or password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/admin/login:
    post:
      tags: [Authentication]
      summary: Login as admin/operator
      operationId: adminLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginPayload'
      responses:
        '200':
          description: Admin login success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid email or password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Authenticated user is not an admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/refresh:
    post:
      tags: [Authentication]
      summary: Refresh token
      operationId: refreshToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenPayload'
      responses:
        '200':
          description: New token pair
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid/expired refresh token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/profile:
    get:
      tags: [Users]
      summary: Get profile
      operationId: getProfile
      security:
        - bearerAuth: []
      responses:
        '200':
          description: User profile detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetailResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags: [Users]
      summary: Update profile
      operationId: updateProfile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfilePayload'
      responses:
        '200':
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetailResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Update failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /consult:
    post:
      tags: [Nutrition]
      summary: Ask AI nutrition consult
      operationId: consult
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConsultRequest'
      responses:
        '200':
          description: AI reply
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsultResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server/AI error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /diet:
    post:
      tags: [Diet]
      summary: Analyze diet image with YOLO and AI scoring
      operationId: analyzeDiet
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
      responses:
        '200':
          description: Analysis result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalorieResponse'
        '400':
          description: Invalid upload/image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: YOLO/AI/server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /diet_record:
    get:
      tags: [Diet]
      summary: Get latest diet records (max 30)
      operationId: getDietRecords
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Diet records list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DietRecordResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /diet_image:
    post:
      tags: [Diet]
      summary: Get analyzed image by record id
      description: Requires bearer token.
      operationId: getDietImage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImageRequest'
      responses:
        '200':
          description: Base64 image payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden or record not owned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Image/record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /record:
    post:
      tags: [System]
      summary: Record a site visit for current date
      operationId: recordVisit
      responses:
        '200':
          description: Visit recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Visit recorded successfully
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /month_stats:
    get:
      tags: [System]
      summary: Get daily visit counts of latest 30 days
      operationId: getMonthStats
      responses:
        '200':
          description: Daily stats
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DailyStatsResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat_check:
    get:
      tags: [Chat]
      summary: Check downstream chat agent availability
      operationId: chatCheck
      responses:
        '200':
          description: Agent is reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProxyChatCheckResponse'
        '500':
          description: Missing configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProxyChatCheckResponse'
        '503':
          description: Agent unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProxyChatCheckResponse'

  /chat:
    post:
      tags: [Chat]
      summary: Proxy chat request to agent service (SSE)
      operationId: chat
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentChatRequest'
      responses:
        '200':
          description: Server-Sent Events stream
          content:
            text/event-stream:
              schema:
                type: string
                example: "data: {\"type\":\"answer\",\"content\":\"...\"}\n\n"
        '400':
          description: Invalid image format/payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server or downstream error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /approve:
    post:
      tags: [Admin]
      summary: Approve or reject downstream agent action
      operationId: approveAgent
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentApproveRequest'
      responses:
        '200':
          description: Pass-through JSON from downstream service
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server/downstream error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /announcements/current:
    get:
      tags: [Knowledge]
      summary: Get the latest published public announcement
      operationId: getCurrentAnnouncement
      responses:
        '200':
          description: Current published announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentAnnouncementResponse'
        '404':
          description: No published announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /news/sync:
    post:
      tags: [Knowledge]
      summary: Trigger downstream FDA news sync
      operationId: newsSync
      responses:
        '200':
          description: News sync result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NewsSyncResponse'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /news:
    get:
      tags: [Knowledge]
      summary: Get paginated local news list
      operationId: listNews
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            format: int64
            minimum: 1
        - in: query
          name: pageSize
          schema:
            type: integer
            format: int64
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: News list response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NewsListResponse'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /news/{id}:
    get:
      tags: [Knowledge]
      summary: Get one local news article
      operationId: getNewsDetail
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: News detail response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NewsDetailResponse'
        '404':
          description: News entry or file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /news-files:
    get:
      tags: [Knowledge]
      summary: List local news markdown files
      operationId: listNewsFiles
      responses:
        '200':
          description: News file list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NewsFilesResponse'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /rag/search:
    get:
      tags: [Knowledge]
      summary: Search local knowledge corpus
      operationId: ragSearchGet
      parameters:
        - in: query
          name: query
          required: true
          schema:
            type: string
        - in: query
          name: top_k
          schema:
            type: integer
            format: int64
            minimum: 1
            maximum: 12
        - in: query
          name: force_refresh
          schema:
            type: boolean
      responses:
        '200':
          description: RAG search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagSearchResponse'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Knowledge]
      summary: Search local knowledge corpus with source filters
      operationId: ragSearchPost
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RagSearchRequest'
      responses:
        '200':
          description: RAG search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagSearchResponse'
        '400':
          description: Invalid payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagSearchValidationError'
        '500':
          description: Server configuration error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Downstream service unavailable or invalid response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/me:
    get:
      tags: [Admin]
      summary: Get current admin identity
      operationId: adminMe
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current admin profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/users:
    get:
      tags: [Admin]
      summary: List admin-visible users
      operationId: adminListUsers
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            format: int64
            minimum: 1
            maximum: 200
        - in: query
          name: offset
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '200':
          description: User list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminUserListItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/users/{user_id}:
    get:
      tags: [Admin]
      summary: Get one user detail for admin console
      operationId: adminGetUserDetail
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDetailResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/route-controls:
    get:
      tags: [Admin]
      summary: List route control flags
      operationId: adminListRouteControls
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Route control list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RouteControlItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/route-controls/{route_key}:
    patch:
      tags: [Admin]
      summary: Update one route control flag
      operationId: adminUpdateRouteControl
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: route_key
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRouteControlPayload'
      responses:
        '200':
          description: Updated route control
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteControlItem'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/announcements:
    get:
      tags: [Admin]
      summary: List announcements for admin console
      operationId: adminListAnnouncements
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            format: int64
            minimum: 1
            maximum: 200
        - in: query
          name: offset
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '200':
          description: Announcement list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AnnouncementItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Admin]
      summary: Create announcement
      operationId: adminCreateAnnouncement
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAnnouncementPayload'
      responses:
        '200':
          description: Created announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnnouncementItem'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/announcements/{id}:
    patch:
      tags: [Admin]
      summary: Update announcement
      operationId: adminUpdateAnnouncement
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAnnouncementPayload'
      responses:
        '200':
          description: Updated announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnnouncementItem'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Announcement not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/announcements/{id}/publish:
    post:
      tags: [Admin]
      summary: Publish announcement
      operationId: adminPublishAnnouncement
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Published announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnnouncementItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Announcement not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/announcements/{id}/archive:
    post:
      tags: [Admin]
      summary: Archive announcement
      operationId: adminArchiveAnnouncement
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Archived announcement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnnouncementItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Announcement not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/rag/documents:
    get:
      tags: [Admin]
      summary: List RAG documents
      operationId: adminListRagDocuments
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            format: int64
            minimum: 1
            maximum: 200
        - in: query
          name: offset
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '200':
          description: RAG document list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RagDocumentItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [Admin]
      summary: Upload RAG document
      operationId: adminUploadRagDocument
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                embeddingModel:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Uploaded RAG document metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentItem'
        '400':
          description: Invalid upload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '413':
          description: File too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/rag/documents/{document_id}:
    get:
      tags: [Admin]
      summary: Get RAG document detail
      operationId: adminGetRagDocument
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: RAG document detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Admin]
      summary: Delete RAG document
      operationId: adminDeleteRagDocument
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deleted RAG document metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/rag/documents/{document_id}/reindex:
    post:
      tags: [Admin]
      summary: Requeue a RAG document for indexing
      operationId: adminReindexRagDocument
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Requeued RAG document metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/rag/documents/{document_id}/file:
    get:
      tags: [Admin]
      summary: Open original RAG document file
      operationId: adminGetRagDocumentFile
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Original file bytes
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            text/plain:
              schema:
                type: string
            text/markdown:
              schema:
                type: string
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/rag/documents/{document_id}/preview:
    get:
      tags: [Admin]
      summary: Preview RAG document content
      operationId: adminGetRagDocumentPreview
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Preview payload for frontend rendering
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentPreviewResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /rag/sources/{document_id}/file:
    get:
      tags: [Knowledge]
      summary: Open a RAG source file for end users
      operationId: getRagSourceFile
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Original file bytes
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            text/plain:
              schema:
                type: string
            text/markdown:
              schema:
                type: string
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /rag/sources/{document_id}/preview:
    get:
      tags: [Knowledge]
      summary: Preview a RAG source for end users
      operationId: getRagSourcePreview
      parameters:
        - in: path
          name: document_id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Preview payload for frontend rendering
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagDocumentPreviewResponse'
        '404':
          description: Document not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat_rooms:
    get:
      tags: [Chat]
      summary: Get chat room list
      operationId: getChatRooms
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Chat rooms
          content:
            application/json:
              schema:
                type: object
                properties:
                  rooms:
                    type: array
                    items:
                      $ref: '#/components/schemas/RoomResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat_room_titles:
    get:
      tags: [Chat]
      summary: Get chat room titles and summaries
      operationId: getChatRoomTitles
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Chat room title list
          content:
            application/json:
              schema:
                type: object
                properties:
                  rooms:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatRoomTitleItem'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /room_history/{room_id}:
    get:
      tags: [Chat]
      summary: Get room history messages
      operationId: getRoomHistory
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: room_id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: History list
          content:
            application/json:
              schema:
                type: object
                properties:
                  history:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatMessage'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /room_history/{room_id}/index/{index}:
    get:
      tags: [Chat]
      summary: Get specific room history detail by index
      operationId: getRoomHistoryByIndex
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: room_id
          required: true
          schema:
            type: string
        - in: path
          name: index
          required: true
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '200':
          description: History detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  detail:
                    $ref: '#/components/schemas/RoomHistoryDetailResponse'
        '400':
          description: Invalid index
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Index out of range
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string

    UserProfile:
      type: object
      required: [id, email, role]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        nickname:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        role:
          type: string
          enum: [user, operator, super_admin]

    AuthResponse:
      type: object
      required: [token, refreshToken, expiresIn, user]
      properties:
        token:
          type: string
        refreshToken:
          type: string
        expiresIn:
          type: integer
          format: int64
        user:
          $ref: '#/components/schemas/UserProfile'

    RegisterPayload:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 6
        nickname:
          type: string
          minLength: 2
          maxLength: 20
          nullable: true
        avatar_url:
          type: string
          nullable: true

    LoginPayload:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    RefreshTokenPayload:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          type: string

    AiConsultationRecord:
      type: object
      required: [id, question, ai_response, created_at]
      properties:
        id:
          type: string
          format: uuid
        question:
          type: string
        ai_response:
          type: string
        created_at:
          type: string

    UserDetailResponse:
      type: object
      required: [id, email, aiConsultations]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        nickname:
          type: string
          nullable: true
        avatarUrl:
          type: string
          nullable: true
        height:
          type: number
          format: double
          nullable: true
        weight:
          type: number
          format: double
          nullable: true
        age:
          type: number
          format: double
          nullable: true
        gender:
          type: string
          nullable: true
        taboo:
          type: array
          nullable: true
          items:
            type: string
        disease:
          type: array
          nullable: true
          items:
            type: string
        aiConsultations:
          type: array
          items:
            $ref: '#/components/schemas/AiConsultationRecord'

    UpdateProfilePayload:
      type: object
      properties:
        nickname:
          type: string
          nullable: true
        height:
          type: number
          format: double
          nullable: true
        weight:
          type: number
          format: double
          nullable: true
        age:
          type: number
          format: double
          nullable: true
        gender:
          type: string
          nullable: true
        taboo:
          type: array
          nullable: true
          items:
            type: string
        disease:
          type: array
          nullable: true
          items:
            type: string

    ConsultRequest:
      type: object
      required: [question]
      properties:
        question:
          type: string

    ConsultResponse:
      type: object
      required: [reply]
      properties:
        reply:
          type: string

    FoodItem:
      type: object
      required: [class, confidence, estimated_weight_g, calories]
      properties:
        class:
          type: string
        confidence:
          type: number
          format: double
        estimated_weight_g:
          type: number
          format: double
        calories:
          type: number
          format: double

    CalorieResponse:
      type: object
      required: [message, total_calories, detected_items, ai_score, ai_comment]
      properties:
        message:
          type: string
        total_calories:
          type: number
          format: double
        detected_items:
          type: array
          items:
            $ref: '#/components/schemas/FoodItem'
        image_base64:
          type: string
          nullable: true
        ai_score:
          type: integer
        ai_comment:
          type: string

    DietRecordResponse:
      type: object
      required: [id, created_at, total_calories]
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
        total_calories:
          type: number
          format: double
        grain_calories:
          type: number
          format: double
          nullable: true
        grain_area:
          type: number
          format: double
          nullable: true
        protein_meat_calories:
          type: number
          format: double
          nullable: true
        protein_meat_area:
          type: number
          format: double
          nullable: true
        protein_bean_calories:
          type: number
          format: double
          nullable: true
        protein_bean_area:
          type: number
          format: double
          nullable: true
        vegetable_calories:
          type: number
          format: double
          nullable: true
        vegetable_area:
          type: number
          format: double
          nullable: true
        fruit_calories:
          type: number
          format: double
          nullable: true
        fruit_area:
          type: number
          format: double
          nullable: true
        dairy_calories:
          type: number
          format: double
          nullable: true
        dairy_area:
          type: number
          format: double
          nullable: true
        nuts_calories:
          type: number
          format: double
          nullable: true
        nuts_area:
          type: number
          format: double
          nullable: true
        ai_health_score:
          type: integer
          nullable: true
        ai_evaluation:
          type: string
          nullable: true

    ImageRequest:
      type: object
      required: [record_id]
      properties:
        record_id:
          type: string
          format: uuid

    ImageResponse:
      type: object
      required: [message]
      properties:
        message:
          type: string
        image_base64:
          type: string
          nullable: true
        original_image_base64:
          type: string
          nullable: true

    DailyStatsResponse:
      type: object
      required: [date, visit_count]
      properties:
        date:
          type: string
        visit_count:
          type: integer

    ProxyChatCheckResponse:
      type: object
      required: [pingUrl, pingOk, proxyChatAvailable]
      properties:
        pingUrl:
          type: string
        pingOk:
          type: boolean
        proxyChatAvailable:
          type: boolean
        pingStatusCode:
          type: integer
          nullable: true
        pingResponse:
          type: string
          nullable: true

    Gemma4HealthResponse:
      type: object
      required: [targetUrl, model, running, message]
      properties:
        targetUrl:
          type: string
        model:
          type: string
        running:
          type: boolean
        httpStatusCode:
          type: integer
          nullable: true
        message:
          type: string

    CurrentAnnouncementResponse:
      type: object
      required: [id, title, content, publishedAt]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        publishedAt:
          type: string
          format: date-time

    AdminUserListItem:
      type: object
      required: [id, email, role]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        nickname:
          type: string
          nullable: true
        avatarUrl:
          type: string
          nullable: true
        role:
          type: string
          enum: [user, operator, super_admin]

    RouteControlItem:
      type: object
      required: [routeKey, isEnabled, isProtected]
      properties:
        routeKey:
          type: string
        isEnabled:
          type: boolean
        reason:
          type: string
          nullable: true
        isProtected:
          type: boolean
        updatedBy:
          type: string
          format: uuid
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true

    UpdateRouteControlPayload:
      type: object
      required: [isEnabled]
      properties:
        isEnabled:
          type: boolean
        reason:
          type: string
          nullable: true

    AnnouncementItem:
      type: object
      required: [id, title, content, status, createdBy, updatedBy, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        status:
          type: string
          enum: [draft, published, archived]
        publishedAt:
          type: string
          format: date-time
          nullable: true
        createdBy:
          type: string
          format: uuid
        updatedBy:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateAnnouncementPayload:
      type: object
      required: [title, content]
      properties:
        title:
          type: string
        content:
          type: string
        status:
          type: string
          enum: [draft, published, archived]
          nullable: true

    UpdateAnnouncementPayload:
      type: object
      properties:
        title:
          type: string
          nullable: true
        content:
          type: string
          nullable: true
        status:
          type: string
          enum: [draft, published, archived]
          nullable: true

    AgentChatRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string
        room_id:
          type: string
          format: uuid
          nullable: true
        user_context:
          type: object
          nullable: true
          additionalProperties: true
        image:
          type: string
          nullable: true
          description: Absolute path, base64, or data URL image payload

    AgentApproveRequest:
      type: object
      required: [approval_id, action]
      properties:
        approval_id:
          type: string
        action:
          type: string
          enum: [approve, reject]

    RoomResponse:
      type: object
      required: [id, title]
      properties:
        id:
          type: string
        title:
          type: string
        last_updated:
          type: string
          format: date-time
          nullable: true

    ChatMessage:
      type: object
      required: [role, content]
      properties:
        role:
          type: string
          example: user
        content:
          type: string
        image_path:
          type: string
          nullable: true
        image_base64:
          type: string
          nullable: true

    ChatRoomTitleItem:
      type: object
      required: [roomId, title, summary]
      properties:
        roomId:
          type: string
        title:
          type: string
        summary:
          oneOf:
            - type: object
              additionalProperties: true
            - type: array
              items:
                type: object
                additionalProperties: true
        lastMessageAt:
          type: string
          format: date-time
          nullable: true

    RoomHistoryDetailResponse:
      type: object
      required: [id, roomId, userId, index]
      properties:
        id:
          type: string
          format: uuid
        roomId:
          type: string
        userId:
          type: string
          format: uuid
        index:
          type: integer
          format: int64
        userMessage:
          type: string
          nullable: true
        imagePath:
          type: string
          nullable: true
        imageBase64:
          type: string
          nullable: true
        aiAnalysisReport:
          type: string
          nullable: true
        dietReport:
          type: object
          nullable: true
          additionalProperties: true
        title:
          type: string
          nullable: true
        summary:
          type: string
          nullable: true
        summaryUpdatedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
          nullable: true

    RagDocumentItem:
      type: object
      required: [id, filename, mimeType, sizeBytes, status, createdAt, updatedAt, fileUrl, previewUrl]
      properties:
        id:
          type: string
          format: uuid
        filename:
          type: string
        mimeType:
          type: string
        sizeBytes:
          type: integer
          format: int64
        chunkCount:
          type: integer
          nullable: true
        embeddingModel:
          type: string
          nullable: true
        status:
          type: string
          enum: [uploaded, processing, ready, failed]
        errorMessage:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        fileUrl:
          type: string
        previewUrl:
          type: string

    RagDocumentPreviewResponse:
      type: object
      required: [documentId, filename, mimeType, previewKind, truncated, fileUrl, previewUrl]
      properties:
        documentId:
          type: string
          format: uuid
        filename:
          type: string
        mimeType:
          type: string
        previewKind:
          type: string
          enum: [text, file]
        content:
          type: string
          nullable: true
        truncated:
          type: boolean
        fileUrl:
          type: string
        previewUrl:
          type: string
        message:
          type: string
          nullable: true

    NewsSyncResponse:
      type: object
      required: [ok, total, newCount, updatedCount, generatedAt]
      properties:
        ok:
          type: boolean
        total:
          type: integer
          format: int64
        newCount:
          type: integer
          format: int64
        updatedCount:
          type: integer
          format: int64
        generatedAt:
          type: string
          format: date-time

    NewsListItem:
      type: object
      required: [id, title]
      properties:
        id:
          type: string
        title:
          type: string
        publishedDate:
          type: string
          nullable: true

    NewsListResponse:
      type: object
      required: [ok, page, pageSize, total, totalPages, items]
      properties:
        ok:
          type: boolean
        page:
          type: integer
          format: int64
        pageSize:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int64
        items:
          type: array
          items:
            $ref: '#/components/schemas/NewsListItem'

    NewsDetailItem:
      type: object
      required: [id, title, sourceUrl, content]
      properties:
        id:
          type: string
        title:
          type: string
        publishedDate:
          type: string
          nullable: true
        sourceUrl:
          type: string
        content:
          type: string

    NewsDetailResponse:
      type: object
      required: [ok, item]
      properties:
        ok:
          type: boolean
        item:
          $ref: '#/components/schemas/NewsDetailItem'

    NewsFilesResponse:
      type: object
      required: [ok, files]
      properties:
        ok:
          type: boolean
        files:
          type: array
          items:
            type: string

    RagSearchRequest:
      type: object
      required: [query]
      properties:
        query:
          type: string
        top_k:
          type: integer
          format: int64
          minimum: 1
          maximum: 12
        source_types:
          type: array
          items:
            type: string
            enum: [nutrition_rules, mohw_news, uploaded_knowledge]
        force_refresh:
          type: boolean

    RagSearchHit:
      type: object
      required: [id, source_type, title, source_path, score, snippet]
      properties:
        id:
          type: string
        source_type:
          type: string
        title:
          type: string
        source_path:
          type: string
        published_date:
          type: string
          nullable: true
        score:
          type: integer
        snippet:
          type: string

    RagSearchResponse:
      type: object
      required: [ok, query, total_hits, hits]
      properties:
        ok:
          type: boolean
        query:
          type: string
        total_hits:
          type: integer
          format: int64
        hits:
          type: array
          items:
            $ref: '#/components/schemas/RagSearchHit'

    RagSearchValidationError:
      type: object
      required: [ok, error, details]
      properties:
        ok:
          type: boolean
        error:
          type: string
        details:
          type: object
          properties:
            formErrors:
              type: array
              items:
                type: string
            fieldErrors:
              type: object
              additionalProperties:
                type: array
                items:
                  type: string

    HealthResponse:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: ok

    DiscordUser:
      type: object
      required: [id, username, discriminator]
      properties:
        id:
          type: string
        username:
          type: string
        discriminator:
          type: string
        avatar:
          type: string
          nullable: true

    DiscordLoginResponse:
      type: object
      required: [message, data]
      properties:
        message:
          type: string
        data:
          $ref: '#/components/schemas/DiscordUser'
